require "spec_helper"

describe Net::HTTP::Request do
  context "with GET as a verb"
  context "with a path"
  context "with a subdomain"
  context "with https"
  context "with http"
  context "with a fragment"
  context "with a query"
  context "with a header"
  context "with two identically named headers"
  context "with a body"
  context "with HTTP version 1.1"
  context "with HTTP version 1.0"
  context "with HTTP version 2.0"
  context "with SPDY"
  context "with QUIC"
end
